import { StyleSheet } from "react-native";

export default StyleSheet.create({
    header: {
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",
        fontWeight: "bold",
    },
    userName: {
        marginTop: 60,
        fontSize: 18,
        color: "#FFF",
    },
    searchButton: {
        padding: 10,
        paddingTop: 40,
        marginTop: 20,
        marginRight: 10,
    },
})